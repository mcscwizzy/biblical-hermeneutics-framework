[pytest]
markers =
    gui: browser-level regression tests for the web UI
    smoke: fast smoke coverage for core app loads
    slow: tests that are intentionally slower or broader in scope
    llm: tests that require a real model response
    visual: tests that inspect rendered layout or 3D output
