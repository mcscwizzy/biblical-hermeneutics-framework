# BHF .bhf-data post-v1.2 cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (- [ ]) syntax for tracking.

**Goal:** Remove only proven-obsolete Commentary v1.2 generation residue while preserving frozen-release, runtime, package, verification, test, ancestry, provenance, and current-lineage behavior.

**Architecture:** Use a deterministic, read-only dependency inventory to construct an exact deletion set. Guard that set against protected namespaces and live consumers, delete only approved paths, then regenerate the repository directory inventory and validate the frozen release, current lineage, deterministic suite, and installed package.

**Tech Stack:** Git, POSIX shell, Python 3.11 via .venv/bin/python, pytest, setuptools/build, and existing BHF Commentary release-verification tools.

**Spec:** docs/superpowers/specs/2026-09-15-bhf-data-post-v1-2-cleanup-design.md

## Global Constraints

- Frozen tag commentary-v1.2 peeled target remains f02122eed9642f173e2be3ad39ac5b979a23b898.
- Frozen manifest identity remains aad968530b371ac05de3529c3f0ac0a59afebc66bfaa21c2fcf4ca6306684c5b.
- Frozen artifact root remains ae31de2fe50c0435387995dd4110579ff2b68ae9f59f9dd1b9f5afc55cb785eb.
- Frozen chapter inventory remains 17c2ecd7eb2ee519b9dce332eb370d97cea42ce75fa0eb5537729c424f4990ae.
- The reported terminal-state identity 91df96a0a79571978a7839a29f9c3b22a2fc73dfce52890e27d52a4a087dc0cf is note-only and is not a cleanup gate; terminal statuses are protected by the frozen manifest identity and terminal-state counts.
- Corpus remains 1,189 canonical, 972 published, 185 source-limited, 26 model-rejected, 6 quality-review, and 0 missing.
- No model/provider calls, commentary generation, CKL mutation, release mutation, or lineage rebaseline.
- Do not modify .bhf-data/bhf-commentary-v1.2, package release data, current-lineage baseline state, finalized runner evidence, or test-required fixtures.
- Any unexpected path, UNKNOWN classification, protected-path overlap, identity mismatch, or verifier dependency is a hard stop.

## Files and responsibilities

- Create: docs/bhf-data-retention.md — retained namespace purposes, consumers, and future disposable categories.
- Modify: docs/commentary-artifact-inventory.json — deterministic post-cleanup inventory generated by its existing tool.
- Modify: docs/commentary-v1.2-reader-idea-ancestry-envelope-v1.md — state that its deleted candidate namespace is historical and no longer present in the working tree.
- Retain: docs/superpowers/specs/2026-09-15-bhf-data-post-v1-2-cleanup-design.md — approved safety design.
- Retain temporarily: /tmp/bhf-data-post-v1-2-delete-set.json — machine-readable proposed deletion inventory; aggregate and rationale are recorded in docs/bhf-data-retention.md.
- Delete only the exact paths enumerated in the temporary deletion inventory; no application or verifier source changes are planned.

---

### Task 1: Reconfirm the baseline and protected identities

**Files:**
- Read-only: Git metadata, .bhf-data/**, docs/commentary-v1.2-release.json

**Interfaces:**
- Consumes: repository at starting SHA 79855b71f09ed7563636e2378bd204ae345bcf92 and the frozen identity constants.
- Produces: baseline metrics and a protected-identity record used by every later task.

- [ ] **Step 1: Record branch, SHA, status, and .bhf-data metrics.**

Run:

~~~
git status --short --branch
git rev-parse HEAD
git rev-parse commentary-v1.2^{commit}
find .bhf-data -type f | wc -l
find .bhf-data -type d | wc -l
du -sb .bhf-data
find .bhf-data -mindepth 1 -maxdepth 1 -printf '%y %f\n' | sort
~~~

Expected: starting SHA 79855b71f09ed7563636e2378bd204ae345bcf92, tag target f02122eed9642f173e2be3ad39ac5b979a23b898, and the previously recorded baseline of 20,015 files, 3,405 directories, and 661,684,718 bytes before this cleanup documentation is counted.

- [ ] **Step 2: Run the frozen release verifier and capture its identity/count output.**

Run:

~~~
.venv/bin/python tools/commentary_v12_freeze.py verify
.venv/bin/python tools/commentary_v12_release.py inventory
.venv/bin/python tools/commentary_v12_rebaseline_lineage.py verify
~~~

Expected: frozen status COMMENTARY_V1_2_FROZEN_READY, reconciliation RECONCILIATION_PASS, and CURRENT_LINEAGE_VALID with the exact identities and counts in the global constraints.

- [ ] **Step 3: Abort if the baseline differs unexpectedly.**

Compare the recorded metrics and verifier identities with the known baseline. Continue only if differences are explained by the approved design/spec files created before cleanup; do not overwrite or normalize any protected artifact.

### Task 2: Generate and guard the machine-readable deletion inventory

**Files:**
- Create temporarily: /tmp/bhf-data-post-v1-2-delete-set.json
- Read-only: framework/, tools/, tests/, .github/workflows/, docs/, and package configuration

**Interfaces:**
- Consumes: baseline metrics, code-reference audit, and the approved design.
- Produces: JSON records with path, size, classification, reason, and dependency_scan_result, plus aggregate file/directory/byte totals.

- [ ] **Step 1: Enumerate the four approved historical candidate namespaces.**

The exact roots are:

~~~
.bhf-data/bhf-commentary-candidates/commentary-output-conformance-v1-d66bea1878bb6c198b08
.bhf-data/bhf-commentary-candidates/reader-idea-ancestry-envelope-v1-b970c53e1dc561f23530
.bhf-data/bhf-commentary-candidates/reader-level-idea-projection-v1-2c6ef478492fe18246a5
.bhf-data/bhf-commentary-candidates/reader-level-idea-projection-v1-aba25de1f5f5a665a8d4
~~~

Classify each file HISTORICAL_REPRODUCIBILITY_ONLY or UNREFERENCED only after confirming no active source, test, runtime, package, verification, or current-lineage consumer. Retain the five-chapter structured-enrichment namespace because release-critical tests load its final report and CKL structured-claim tests load its target constants.

- [ ] **Step 2: Enumerate the two non-final corpus-runner sessions.**

The exact roots are:

~~~
.bhf-data/bhf-commentary-candidates/commentary-v1.2-enrichment/corpus-runner/runs/batch-f69143177d9d0682
.bhf-data/bhf-commentary-candidates/commentary-v1.2-enrichment/corpus-runner/runs/session-batch-2e86556dfb1e5a52
~~~

Record their incomplete/superseded statuses and confirm they contain no finalized result set required by tools/commentary_v12_release.py. Retain all 15 finalized sessions and their result, state, manifest, finalization-audit, required raw-response, and renderer metadata files.

- [ ] **Step 3: Enumerate generation-only runner material with exact file rules.**

Within .bhf-data/bhf-commentary-candidates/commentary-v1.2-enrichment/corpus-runner/runs/, include only:

~~~
chapters/*/renderer-receipt.json
chapters/*/renderer-input/*/system_prompt.txt
chapters/*/renderer-input/*/user_prompt.txt
~~~

Retain result.json, state.json, run manifest.json, run finalization files, finalize-audit.json sidecars, raw-response.bin, and renderer-input/*/metadata.json. Retained metadata is the evidence/synthesis/binding identity used by reconciliation; prompt bodies and optional receipts are generation history.

- [ ] **Step 4: Enumerate canary staging residue and the exact duplicate.**

Include:

~~~
.bhf-data/bhf-commentary-candidates/commentary-v1.2-enrichment/canary/responses/raw
.bhf-data/bhf-commentary-candidates/commentary-v1.2-enrichment/canary/responses/raw-rerun-*
.bhf-data/bhf-commentary-candidates/commentary-v1.2-enrichment/canary/responses/rejected
.bhf-data/bhf-commentary-candidates/commentary-v1.2-enrichment/canary/manual-render
.bhf-data/biblical-hermeneutics-framework/.bhf-data/bhf-commentary-candidates/commentary-v1.2-enrichment/corpus-runner/runs/session-batch-eff892f5ceb94cfe/chapters/genesis_018/raw-response.bin
~~~

Confirm the duplicate SHA-256 equals the canonical genesis_018/raw-response.bin before including it. Retain canary prompts, accepted responses, synthesis, preflight/gate state, audit outputs, and import/comparison contracts.

- [ ] **Step 5: Write the inventory and assert its exact scope.**

For every candidate path, write a JSON record containing:

~~~
{
  "path": "relative/path",
  "size": 0,
  "classification": "OBSOLETE_GENERATION_INTERMEDIATE",
  "reason": "specific proven dependency result",
  "dependency_scan_result": "no runtime/package/verification/test/lineage consumer"
}
~~~

Assert that the inventory contains no path below .bhf-data/bhf-commentary-v1.2, no current-lineage path, no finalized runner evidence, no package data, no SQLite runtime store, and no UNKNOWN classification. Assert the approved narrowed estimate of 3,046 files and 43,239,735 bytes; a live mismatch is a hard stop.

### Task 3: Dry-validate the deletion set against consumers

**Files:**
- Read-only: deletion inventory, source, tests, workflows, docs, manifests, descriptors, and package configuration

**Interfaces:**
- Consumes: /tmp/bhf-data-post-v1-2-delete-set.json.
- Produces: a pass/fail guard proving the set excludes protected dependencies.

- [ ] **Step 1: Re-run dynamic-path searches for every proposed namespace.**

Inspect constants and path construction in framework/commentary/v12_current_lineage.py, framework/commentary/v12_corpus.py, tools/commentary_v12_release.py, tools/commentary_v12_freeze.py, tools/commentary_artifact_inventory.py, and the release-critical workflow. Search tests/, framework/, tools/, .github/workflows/, package configuration, and docs while excluding .bhf-data/** from the search body. Treat docs as historical references requiring documentation updates, not as silently ignored dependencies.

- [ ] **Step 2: Prove runner requirements.**

Read _validate_runner_result, _effective_audits, and runner globbing in tools/commentary_v12_release.py. Verify the proposed set retains every input needed for 1,114 runner terminal results and removes only optional receipts, prompt bodies, and two non-final sessions.

- [ ] **Step 3: Verify package independence from development data.**

Inspect pyproject.toml, framework/commentary/repository.py, bhf_agent/runtime_paths.py, and package-data declarations. Confirm v1.2 package loading resolves to bhf_agent/data/commentary-v1.2, not candidate .bhf-data.

- [ ] **Step 4: Abort on any dependency disagreement.**

Do not mutate files if a proposed path is referenced by a verifier, test fixture, package/runtime import, current-lineage baseline, ancestry/provenance contract, or frozen release manifest.

### Task 4: Apply only the guarded cleanup

**Files:**
- Delete: exactly the paths in /tmp/bhf-data-post-v1-2-delete-set.json
- Retain: all paths excluded by the protected-path assertions

**Interfaces:**
- Consumes: validated deletion inventory.
- Produces: working tree containing the frozen release and all retained dependencies, with only approved deletions.

- [ ] **Step 1: Recheck the working tree and inventory immediately before deletion.**

Run git status --short, verify every inventory path exists with its recorded size, and verify protected identities again. Stop if the inventory is stale.

- [ ] **Step 2: Delete exact inventory paths using a guarded manifest-driven operation.**

Use the machine-readable inventory to remove only listed files/directories. Do not use broad .bhf-data globs, recursive deletion from a repository root, or cleanup of unknown/ignored runtime stores. Record the actual removed file count, directory count, and bytes.

- [ ] **Step 3: Remove empty parents only inside approved deleted namespaces.**

Do not prune shared .bhf-data or candidate parent directories unless they are empty after approved paths are removed. Retain the candidate root and all active namespaces.

- [ ] **Step 4: Confirm no protected release files changed.**

Run git diff --name-status -- .bhf-data/bhf-commentary-v1.2 and compare frozen release checksums. Expected: no changes under the frozen release directory.

### Task 5: Rebuild inventory documentation and retention policy

**Files:**
- Create: docs/bhf-data-retention.md
- Modify: docs/commentary-artifact-inventory.json
- Modify: docs/commentary-v1.2-reader-idea-ancestry-envelope-v1.md
- Retain unchanged: .gitignore unless a narrowly proven new scratch path is identified

**Interfaces:**
- Consumes: final filesystem inventory, dependency classifications, actual deletion totals, and the existing artifact inventory tool.
- Produces: current directory inventory and human-readable retention policy.

- [ ] **Step 1: Regenerate the existing directory inventory.**

Run:

~~~
.venv/bin/python tools/commentary_artifact_inventory.py
~~~

Confirm deleted roots no longer appear as present records, the retained five-chapter artifact remains recorded, and no release identity in the generated document changes.

- [ ] **Step 2: Update the historical ancestry-envelope report.**

Change only the statement that presents the deleted candidate namespace as an immutable working-tree artifact. State that it was a historical diagnostic candidate removed by the post-freeze cleanup, while the source projection namespace used by current tests and lineage remains retained. Do not alter the diagnostic’s recorded result identities.

- [ ] **Step 3: Create the retention document.**

Document each major retained namespace with path, purpose, retention reason, referenced by, and safe to delete. Cover frozen release, runtime-compatible legacy releases, package data, current lineage, finalized corpus runner, retained validation/provenance/ancestry fixtures, canary gate/audit state, local SQLite runtime stores, and intentionally retained historical qualification inputs. Record removed categories, exact deletion totals, and future disposable categories: incomplete runner sessions, prompt bodies, optional receipts, raw retry staging, rejected/manual-render scratch, and unreferenced diagnostic roots after dependency review.

- [ ] **Step 4: Inspect .gitignore and make no broad rule.**

Retain existing narrow rules. Add nothing unless a new path is proven transient, non-authoritative, and safe to ignore without hiding release-critical evidence.

### Task 6: Run release, lineage, deterministic, and package verification

**Files:**
- Read-only verification of cleaned repository and built package

**Interfaces:**
- Consumes: cleaned repository and retained .bhf-data.
- Produces: verification evidence for the final report.

- [ ] **Step 1: Run frozen release and reconciliation checks.**

Run:

~~~
.venv/bin/python tools/commentary_v12_freeze.py verify
.venv/bin/python tools/commentary_v12_release.py inventory
~~~

Expected: exact manifest, artifact-root, inventory, terminal-state identities and counts from the global constraints; statuses COMMENTARY_V1_2_FROZEN_READY and RECONCILIATION_PASS.

- [ ] **Step 2: Run current source-lineage verification.**

Run:

~~~
.venv/bin/python tools/commentary_v12_rebaseline_lineage.py verify
~~~

Expected: CURRENT_LINEAGE_VALID and unchanged current-lineage manifest identity.

- [ ] **Step 3: Run the release-critical deterministic suite.**

Run:

~~~
.venv/bin/pytest -q \
  tests/test_commentary_v12_freeze.py \
  tests/test_commentary_v12_release_promotion.py \
  tests/test_commentary_v12_reader_integration.py \
  tests/test_commentary_v12_corpus_runner.py \
  tests/test_commentary_v12_current_lineage.py \
  tests/test_commentary_v12_codex_cli.py \
  tests/test_commentary_v12_validation.py \
  tests/test_commentary_v12_external_import.py \
  tests/test_commentary_v12_prompt_18.py \
  tests/test_commentary_v12_reader_idea_ancestry_envelope.py \
  tests/test_commentary_v12_reader_provenance_binding.py \
  tests/test_commentary_v12_synthesis.py \
  tests/test_commentary_output_conformance.py \
  tests/test_commentary_availability.py \
  tests/test_commentary_canonical_runtime.py \
  tests/canonical_library/test_manifest.py \
  tests/canonical_library/test_release.py \
  tests/canonical_library/test_schema_package.py
~~~

Expected: all tests pass without provider credentials or generation.

- [ ] **Step 4: Build and smoke-test the installed package.**

Run:

~~~
.venv/bin/python -m build
rm -rf /tmp/bhf-commentary-v12-cleanup-venv
.venv/bin/python -m venv /tmp/bhf-commentary-v12-cleanup-venv
/tmp/bhf-commentary-v12-cleanup-venv/bin/python -m pip install dist/*.whl
cd /tmp
BHF_COMMENTARY_RELEASE=commentary-v1.2 /tmp/bhf-commentary-v12-cleanup-venv/bin/python -c 'from bhf_agent.chapter_commentary.release import release_diagnostics; from bhf_agent.chapter_commentary.storage import load_commentary; from bhf_agent.runtime_paths import packaged_commentary_storage_path; root=packaged_commentary_storage_path("commentary-v1.2"); d=release_diagnostics(root, "commentary-v1.2"); assert d["checksum_status"] == "valid"; assert d["published_commentary_count"] == 972; assert d["validated_population_count"] == 1189; assert d["unpublished_validated_count"] == 217; assert load_commentary(root, "Genesis", 1) is not None; print(d)'
~~~

Expected: package loads v1.2 from packaged data, reports valid checksums, 972 published, 1,189 validated, and does not require candidate .bhf-data.

### Task 7: Final audit and dedicated commit

**Files:**
- Modify: docs/bhf-data-retention.md with final measured totals and verification results
- Include: approved design, plan, retention document, regenerated artifact inventory, and historical-document correction
- Delete: only approved inventory paths

**Interfaces:**
- Consumes: verification outputs and final filesystem metrics.
- Produces: one dedicated cleanup commit and final report data.

- [ ] **Step 1: Measure final .bhf-data and corpus-runner totals.**

Run the same find/du -sb baseline commands and measure corpus-runner/runs separately. Report files, directories, bytes, removed totals, percent reduction, 17 initial sessions, 15 retained, and 2 removed.

- [ ] **Step 2: Check final diff scope and frozen tag.**

Run:

~~~
git status --short
git diff --check
git diff --name-status
git rev-parse commentary-v1.2^{commit}
~~~

Expected: only approved .bhf-data deletions and documentation changes; no source, package release, CKL, lineage baseline, or tag changes.

- [ ] **Step 3: Update final retention metrics and verification results.**

Record exact before/after values, removed categories, retained categories, verification statuses, LLM/provider calls: 0, and Generation performed: NO in docs/bhf-data-retention.md.

- [ ] **Step 4: Commit the complete cleanup as one dedicated change.**

Run:

~~~
git add -A -- .bhf-data docs/bhf-data-retention.md docs/commentary-artifact-inventory.json docs/commentary-v1.2-reader-idea-ancestry-envelope-v1.md docs/superpowers/specs/2026-09-15-bhf-data-post-v1-2-cleanup-design.md docs/superpowers/plans/2026-09-15-bhf-data-post-v1-2-cleanup.md
git commit -m "Prune obsolete Commentary v1.2 generation data"
~~~

Expected: dedicated post-freeze cleanup commit after all verification passes; commentary-v1.2 remains pointed at the frozen commit.
